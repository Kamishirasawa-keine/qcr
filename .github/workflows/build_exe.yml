name: build

on: [push, pull_request]

jobs:
    build:
        runs-on: windows-latest
        steps:
            - name: checkout
              uses: actions/checkout@v4.1.7
              with:
                fetch-depth: 0

            - name: setup dotnet
              uses: actions/setup-dotnet@v4.0.1
            
            - name: setup msbuild
              uses: microsoft/setup-msbuild@v2

            - name: restore
              run: msbuild qcre.sln /t:Restore /p:Configuration=Debug 

            - name: build
              run: msbuild qcre.sln /p:Configuration=Debug 

            - name: upload artifacts
              uses: actions/upload-artifact@v4
              with:
                name: binary files
                path: bin/Debug/net8.0-windows
    